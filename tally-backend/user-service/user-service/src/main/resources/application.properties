# Application name
spring.application.name=user-service

# Server port
server.port=8082

# Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/tallydb
spring.datasource.username=postgres
spring.datasource.password=tally123

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Show detailed errors
server.error.include-message=always

# JWT secret key (must be stable — changing this invalidates all active sessions)
jwt.secret=TallyAppSecretKey2026KNUSTGroupProjectSecureKey

# Paystack (test mode)
# Get real test keys from dashboard.paystack.com > Settings > API Keys and replace
# the placeholders below. The public key also needs replacing in the mobile app
# (tally-mobile/services/paystack.ts). Move to environment variables before production.
paystack.secret.key=sk_test_91aabe97127538c15f187eeb5b6e84f368d45b75
paystack.public.key=pk_test_a6dabace78597c9cdfa3310c0be8efdf77091f5e
paystack.base.url=https://api.paystack.co

# MTN MoMo Sandbox
# Note: MoMo sandbox balance endpoint may return 503 occasionally — this is normal sandbox behavior.
# The production API is more reliable. The /api/momo/balance endpoint handles 503 gracefully
# by returning a fallback response with status: "unavailable" instead of an error.
# WARNING: Move these to environment variables before production deployment
# Example: export MOMO_SUBSCRIPTION_KEY=... and use ${MOMO_SUBSCRIPTION_KEY} here
momo.subscription.key=69d64a44de7b47c89e8d64655d3f2483
momo.api.user=6c3e4b5a-2d1f-4e8a-9b7c-0d5e6f7a8b9c
momo.api.key=fb794d3442e94dd7a2ffc08d6992420e
momo.base.url=https://sandbox.momodeveloper.mtn.com
momo.environment=sandbox
momo.currency=EUR
momo.callback.url=http://localhost:8082/api/momo/callback