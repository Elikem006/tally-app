# Spring Boot configuration — tracked in git, placeholder values only.
# For Railway: set each ${VAR_NAME} as an environment variable in the Railway dashboard.
# For local dev: override values in application-local.properties (gitignored),
#   then run with SPRING_PROFILES_ACTIVE=local.

spring.application.name=user-service

# Railway injects PORT automatically; 8082 is the local fallback
server.port=${PORT:8082}

# ---------------------------------------------------------------------------
# Database — Neon PostgreSQL
# DB_HOST example: ep-<name>.<region>.aws.neon.tech
# sslmode=require is mandatory for Neon.
# ---------------------------------------------------------------------------
spring.datasource.url=jdbc:postgresql://${DB_HOST}/${DB_NAME}?sslmode=require
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.jdbc.use_get_generated_keys=true

# DDL_AUTO: 'update' for dev (auto-creates tables), 'validate' for production
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.open-in-view=false

# INCLUDE_ERROR_MESSAGES: 'always' for dev, 'never' for production
server.error.include-message=${INCLUDE_ERROR_MESSAGES:always}

# ---------------------------------------------------------------------------
# JWT — must be stable; rotating this invalidates all active sessions
# ---------------------------------------------------------------------------
jwt.secret=${JWT_SECRET}

# ---------------------------------------------------------------------------
# MTN MoMo Collections
# ---------------------------------------------------------------------------
momo.subscription.key=${MOMO_SUBSCRIPTION_KEY}
momo.api.user=${MOMO_API_USER}
momo.api.key=${MOMO_API_KEY}
momo.base.url=${MOMO_BASE_URL:https://sandbox.momodeveloper.mtn.com}
momo.environment=${MOMO_ENVIRONMENT:sandbox}
momo.currency=${MOMO_CURRENCY:EUR}
momo.callback.url=${MOMO_CALLBACK_URL:http://localhost:8082/api/momo/callback}

# ---------------------------------------------------------------------------
# MTN MoMo Disbursements
# ---------------------------------------------------------------------------
momo.disbursement.subscription.key=${MOMO_DISBURSEMENT_SUBSCRIPTION_KEY}
momo.disbursement.api.user=${MOMO_DISBURSEMENT_API_USER}
momo.disbursement.api.key=${MOMO_DISBURSEMENT_API_KEY}
momo.disbursement.base.url=${MOMO_DISBURSEMENT_BASE_URL:https://sandbox.momodeveloper.mtn.com}

# ---------------------------------------------------------------------------
# CORS — set ALLOWED_ORIGINS to your Railway URL in production
# ---------------------------------------------------------------------------
app.cors.allowed-origins=${ALLOWED_ORIGINS:*}
